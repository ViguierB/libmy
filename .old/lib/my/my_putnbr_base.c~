/*
** my_putnbr_base.c for my_putnbr_base.c in /home/benji_epitech/workdir/d6
** 
** Made by Benjamin Viguier
** Login   <benji_epitech@epitech.net>
** 
** Started on  Mon Oct 10 15:06:43 2016 Benjamin Viguier
** Last update Tue Oct 11 09:27:23 2016 Benjamin Viguier
*/

int my_putchar(char c);
int my_strlen(char *str);

static void rec_putnbr_base(unsigned nb, char *base, int lbase);

int my_putnbr_base(int nb, char *base)
{
  int	lbase;

  lbase = my_strlen(base);
  if (nb < 0)
    {
      my_putchar('-');
      rec_putnbr_base(- (unsigned) nb, base, lbase);
    }
  else
    {
      rec_putnbr_base((unsigned) nb, base, lbase);
    }
  return (0);
}

static void rec_putnbr_base(unsigned nb, char *base, int lbase)
{
  if (nb < (unsigned) lbase)
    my_putchar(base[nb]);
  else
    {
      rec_putnbr_base(nb / lbase, base, lbase);
      my_putchar(base[nb % lbase]);
    }
}
